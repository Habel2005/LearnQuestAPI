{
    "builds": [
      {
        "src": "main.py",
        "use": "@railway/python",
        "config": {
          "serverCommand": "uvicorn main:app --host 0.0.0.0 --port ${PORT}"
        }
      }
    ]
  }