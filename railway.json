{
  "build": {
    "nixpacksPlan": {
      "providers": ["python"],
      "variables": {
        "PYTHON_VERSION": "3.10"
      }
    }
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 30"
  }
}